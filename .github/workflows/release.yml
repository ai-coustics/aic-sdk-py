name: Release

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            targets: x86_64-unknown-linux-gnu aarch64-unknown-linux-gnu
            interpreters: '3.10 3.11 3.12 3.13 3.14'
          - os: macos-latest
            targets: x86_64-apple-darwin aarch64-apple-darwin
            interpreters: '3.10 3.11 3.12 3.13 3.14'
          - os: windows-latest
            targets: x86_64-pc-windows-msvc aarch64-pc-windows-msvc
            interpreters: '3.10 3.11 3.12 3.13 3.14'
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.targets }}

      - name: Build wheels
        shell: bash
        run: |
          for target in ${{ matrix.targets }}; do
            echo "Building for $target"
            uvx maturin build --release --target $target --out dist --interpreter ${{ matrix.interpreters }} --zig
          done

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: dist

  sdist:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Build sdist
        run: uvx maturin sdist --out dist

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Release
    runs-on: ubuntu-22.04
    needs: [build, sdist]
    environment: publish
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -la dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
