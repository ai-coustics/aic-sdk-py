name: Build and Run Integration Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build and test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            targets: x86_64-unknown-linux-gnu aarch64-unknown-linux-gnu
            interpreters: '3.10 3.11 3.12 3.13 3.14'
            test-arch: x86_64
            use-zig: true
          - os: macos-latest
            targets: x86_64-apple-darwin aarch64-apple-darwin
            interpreters: '3.10 3.11 3.12 3.13 3.14'
            test-arch: arm64
            use-zig: false
          - os: windows-latest
            interpreters: '3.10 3.11 3.12 3.13 3.14'
            test-arch: amd64
            use-zig: false
          - os: windows-11-arm
            interpreters: '3.11 3.12 3.13 3.14'
            test-arch: arm64
            use-zig: false

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.targets }}

      - name: Build wheels
        shell: bash
        run: |
          if [ "${{ matrix.use-zig }}" = "true" ]; then
            MATURIN="uvx --from maturin[zig] maturin build --zig"
          else
            MATURIN="uvx maturin build"
          fi
          if [ -n "${{ matrix.targets }}" ]; then
            for target in ${{ matrix.targets }}; do
              echo "Building for $target"
              $MATURIN --release --target $target --out dist --interpreter ${{ matrix.interpreters }}
            done
          else
            echo "Building for native target"
            $MATURIN --release --out dist --interpreter ${{ matrix.interpreters }}
          fi

      - name: Test wheels
        shell: bash
        env:
          AIC_SDK_LICENSE: ${{ secrets.AIC_SDK_LICENSE }}
        run: |
          for version in ${{ matrix.interpreters }}; do
            echo "Testing Python $version"
            uv venv --python $version .venv-$version
            VIRTUAL_ENV=.venv-$version uv pip install dist/*cp${version//.}*${{ matrix.test-arch }}*.whl
            uv run --python $version examples/basic.py
            uv run --python $version examples/basic_async.py
          done
