# Install the package in editable mode with its runtime deps
-e .

# Testing
pytest>=7.0
pytest-cov>=4.0

# Optional: integration tests load .env automatically if available
python-dotenv>=1.0

# Docs (local build/serve)
mkdocs>=1.5
mkdocs-material>=9.0
mkdocstrings-python>=1.7

